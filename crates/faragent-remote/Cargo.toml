[package]
name = "faragent-remote"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false
description = "FarAgent remote-side protocols: POSIX and Windows script builders and parsers"

[dependencies]
faragent-core = { path = "../faragent-core" }
anyhow.workspace = true
base64.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
pretty_assertions.workspace = true
# The fallback channel's tests parse the script's frames with the very parser
# the helper's frames go through, which is the point: one wire format, two
# implementations. Dev-only — nothing here is linked into the crate.
faragent-helper = { path = "../faragent-helper" }
